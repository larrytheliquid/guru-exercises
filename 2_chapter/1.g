Include "../../../guru-lang/lib/subtract.g".
% subtract: descends down the recursive constructor until the base constructor is reached, but no more
Interpret (subtract five two).
Interpret (subtract one three).

Include "../../../guru-lang/lib/minmax.g".
% min: lesser term
Interpret (min one zero).
Interpret (min five six).
% max: greater term
Interpret (max one zero).
Interpret (max five six).


